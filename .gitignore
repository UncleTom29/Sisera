__pycache__/
*.pyc
.venv/
.env
.env.*
!.env.example
*.db
*.db-wal
*.db-shm
.pytest_cache/
.ruff_cache/
uv.lock.bak

# OS
.DS_Store

# Telegram / auth session artifacts (revoke any that were ever committed)
*.session
*.session-journal

# Secrets — never commit keys, wallets, cookies, sessions, or databases
*.key
*.pem
secrets.env

# JavaScript / TypeScript
node_modules/
.turbo/
.next/
dist/
build/
*.tsbuildinfo

# Local runtime state (SQLite files on dev boot; Postgres in prod)
var/

# Internal and sensitive documentation (public repo safety)
docs/
docs/*.md
ecosystem.config.cjs
scripts/setup-contabo-vps.sh


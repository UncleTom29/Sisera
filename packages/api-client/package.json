{
  "name": "@sisera/api-client",
  "version": "0.1.0",
  "description": "Shared TypeScript client for the Sisera /api/v1 backend (web + mobile)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
